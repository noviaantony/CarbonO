# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: CarbonO CI/CD 

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest
    environment: 
      name: Production
      url: https://github.com
    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK 18
      uses: actions/setup-java@v3
      with:
        java-version: '18'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven and push image
      env:
          CARBONO_USERNAME: ${{ secrets.CARBONO_USERNAME  }}
          CARBONO_PASSWORD: ${{ secrets.CARBONO_PASSWORD  }}
      run: |
        cd ./carbonO/user
        mvn clean package -D dockerfile.useMavenSettingsForAuth=true dockerfile:push
#     - name: Build & push Docker image
#       uses: mr-smithers-excellent/docker-build-push@v5
#       with:
#         image: newbieshine/springboot-images-new
#         tags: latest
#         registry: docker.io
#         dockerfile: Dockerfile
#         username: ${{ secrets.DOCKER_USERNAME }}
#         password: ${{ secrets.DOCKER_PASSWORD }}
